<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title><%- pageTitle %></title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="/css/styles.css">
  </head>
  <body>

  <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container">
      <div class="navbar-header">
        <a class="navbar-brand mb-0 h1"href="/home">FreshCode</a>
      </div>

      <div class="links">
        <ul class="navbar-nav ml-auto">
          <% for(let i = 0; i < contents.length; i++){ %>
          <%   if(i == selected) { %>
            <li class="nav-item active">
          <%   } else { %>
            <li class="nav-item">
          <%   } %>
            <a class="nav-link" href="/<%-contents[i]['title']%>">
              <%- contents[i]["title"] %>
            </a>
          </li>
          <%   } %>
        </ul>
        <% if(selected == 3) { %>
          <button onclick="location.href='/compose'" class="new-post new-post-active" type="button" name="button">POST</button>
        <% } else { %>
          <button onclick="location.href='/compose'" class="new-post" type="button" name="button">POST</button>
        <% } %>
      </div>
    </div>
  </nav>

  <div class="container">
